// app/(panel)/diagnostics/page.tsx — thin route wrapper keeps the explicit route while the module grows behind shared framing.
import { createModulePage } from "@/lib/module-pages";

export default createModulePage("diagnostics");